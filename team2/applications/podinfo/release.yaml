apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: team2-podinfo-helm
spec:
  destination:
    name: staging
  source:
    repoURL: 'https://stefanprodan.github.io/podinfo'
    targetRevision: x.x.x
    chart: podinfo
  project: team2
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
